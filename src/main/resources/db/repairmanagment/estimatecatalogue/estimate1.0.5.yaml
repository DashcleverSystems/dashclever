databaseChangeLog:
  - changeSet:
      id: estimate1.0.5
      author: pawel
      changes:
        - addColumn:
            tableName: RM_ESTIMATECATALOGUE_ESTIMATE
            columns:
              - column:
                  name: reports_id
                  type: UUID
                  constraints:
                    nullable: true
        - addForeignKeyConstraint:
            baseTableName: RM_ESTIMATECATALOGUE_ESTIMATE
            baseColumnNames: reports_id
            referencedTableName: RM_ESTIMATECATALOGUE_ESTIMATEREPORT
            referencedColumnNames: id
            constraintName: FK_ESTIMATE_REPORTS
            onDelete: CASCADE
